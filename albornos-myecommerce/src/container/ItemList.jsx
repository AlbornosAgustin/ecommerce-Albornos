import React from 'react'
import item from './item'

const itemList = (prop) => {
   
    return (
        <div>
           Remeras.map( prop ={<item/>}  ) 
        </div>
    )
}

export default itemList
